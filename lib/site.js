export const installUrl =
  process.env.NEXT_PUBLIC_INSTALL_URL || "#browser-support";

export const proUrl =
  process.env.NEXT_PUBLIC_PRO_URL || "#pricing";

export const contactEmail =
  process.env.NEXT_PUBLIC_CONTACT_EMAIL || "hello@studycapture.app";

export const contactMailto = `mailto:${contactEmail}`;
